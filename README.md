# URL-Shortener_FastAPI
This is a simple and lightweight URL Shortener API built using Python and FastAPI. The application accepts long URLs from users, generates a unique short code, stores the mapping in a JSON file, and redirects users to the original URL when the short link is accessed.
